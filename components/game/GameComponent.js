import React from "react";
import { Button, View, Text } from "react-native";

const GameComponent = () => {
    return (
        <View>
            
        </View>
    );
};

export default GameComponent;